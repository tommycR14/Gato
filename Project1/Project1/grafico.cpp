#include "grafico.h"
grafico::grafico() {
	//window = new sf::RenderWindow(sf::VideoMode(1200, 800), "TIC - TAC - TOE");

}

void grafico::gameLoop() {
	//while (window->isOpen()) {
		//sf::Texture t1;
		//sf::Sprite s1;
		//window->display();
		////t1.loadFromFile("/imagenes/paisaje");
		////s1.setTexture(t1);
		//s1.setPosition(100, 100);
		////window->draw(s1);
	//}
}